machine:
  features:
    hostDNS:
      enabled: true
      forwardKubeDNSToHost: false # required to allow Cilium DNS to work properly 
  network:
    interfaces:
      - deviceSelector:
          physical: true
        dhcp: true
        mtu: 9000
    extraHostEntries:
      - ip: 10.20.10.1 
        aliases:
          - spiff.rockyroad.rocks
  time:
    servers:
      - time.cloudflare.com
